.navigation__list
  border-top 1px solid darken(themeColor, changeColor * 2)
  list-style-type none
  padding 0
  +maxScreen(navigationBreakPoint)
    align-items center
    display flex
    flex-direction column
    height 100%
    justify-content center
    &>li
      padding 10px 0
